import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bells.exam.scheduling.system',
  appName: 'bells-exam-scheduling-system',
  webDir: 'dist'
};

export default config;
